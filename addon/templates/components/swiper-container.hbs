{{#if parallax}}
  <div class="parallax-bg" data-swiper-parallax={{bgParallaxSpeed}}></div>
{{/if}}

<div class="swiper-wrapper">
  {{yield}}
</div>

{{#if pagination}}
  <div class="swiper-pagination"></div>
{{/if}}

{{#if navigation}}
  <div class="swiper-button-next"></div>
  <div class="swiper-button-prev"></div>
{{/if}}
